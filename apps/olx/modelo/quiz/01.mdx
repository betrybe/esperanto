---
habilidades: Definir o `npm` como um gerenciador de pacotes, ou seja, ferramenta que nos permite criar projetos, nossas aplicações, e instalar dependências neles
---

>>Coloque o enunciado entra `<<`  
( )As alternativas erradas são demarcadas como `( )` {{a explicação da alternativa fica entre `{{}}`}}
(x)A questões certa é demarcada com `(x)`{{essa é a questão certa}}
( )``` Por enquanto código em bloco não suportar linguagem 
 E precisa começar na mesma linha do '`'`'`'
```